# Snapdragon Copilot PC Requirements
# For 4-bit quantization of GPT-OSS-20B

# Core ML frameworks
torch>=1.13.0
torchvision>=0.14.0
torchaudio>=0.13.0

# Transformers and model handling
transformers>=4.30.0
safetensors>=0.3.0
accelerate>=0.20.0
tokenizers>=0.13.0

# Quantization specific
# Note: Use CPU-only PyTorch for ARM64 compatibility

# Data processing
numpy>=1.21.0
scipy>=1.9.0

# Storage and serialization
huggingface_hub>=0.15.0
datasets>=2.12.0

# Progress and utilities
tqdm>=4.64.0
psutil>=5.9.0

# Development and testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: ONNX for deployment
# onnx>=1.14.0
# onnxruntime>=1.15.0

# ARM64 specific optimizations
# These may need to be compiled from source on ARM64
# mkl-devel  # Intel MKL (not available on ARM64)
# openblas-devel  # Alternative BLAS for ARM64